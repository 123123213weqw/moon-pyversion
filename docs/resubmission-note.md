# 重新提交备注（核实后粘贴）

> 根据代码体量与集成场景方面的初审意见，已更新 `123123213weqw/moon-pyversion` 的
> 最新 `main` 分支。项目当前并非仅有 PEP 440 版本解析：有效生产 MoonBit 源码为
> **9,984 行 / 13 文件**，测试为 **7,460 行 / 300 个测试块**。统计脚本
> `python -B tools/source_metrics.py` 将生产源码与测试、示例、生成语料及 Python
> 验证工具分开，便于复核；这些数字是仓库总量，不冒称全部为本次新增。
>
> 本轮进一步修正跨制品审计的真实决策链：索引有较新候选时仍选择锁定版本，并核对
> 所选文件名及索引、锁文件两侧的 sha256 声明。新增 `audit_bundle`，可用同一锁文件
> 审计多个项目，识别重复输入和未覆盖的适用锁条目。`examples/bundle-audit` 使用
> Flask 与 Jinja2 的真实 PyPI 元数据和索引响应，配合明确标注为场景输入的锁文件，
> 在 CPython 3.11 / Linux 目标下得到两包 `Ready`；负向测试覆盖摘要漂移、锁文件
> 缺项等情况。复现方法和不做范围见 `docs/bundle-audit-scenario.md`。
>
> 项目现有 300 个测试在 wasm、wasm-gc、js、native 四后端通过；固定
> `packaging==26.3` 的 122,640 条差分记录为 0 不一致，43 项故障注入均被检测。
> 申报书已按“项目定位—核心功能—真实场景—验收边界—来源与规划”重写。烦请以
> 最新提交和对应的绿色 CI 重新审核。谢谢。

提交前由本人核对：仓库地址、最新提交与 CI、申报书正文，以及 MoonCakes 的实际发布
版本。目前注册表独立验证的是 **0.1.0**；仓库 **0.2.0 尚未发布**，不得写成已发布。
